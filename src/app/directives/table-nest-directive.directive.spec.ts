import { TableNestDirectiveDirective } from './table-nest-directive.directive';

describe('TableNestDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TableNestDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
